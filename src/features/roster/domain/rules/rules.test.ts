import {describe,it,expect} from 'vitest';import {validateRoster} from './rules';import type {Staff,Ward,RosterCell} from '../entities';const s:Staff[]=[{id:'1',name:'Priya',grade:'SENIOR',icuCertExpiry:'2027-01-01',leaveDates:[]}];const w:Ward={code:'ICU',name:'ICU',requirements:{D:{total:1,seniors:1},E:{total:1,seniors:1},N:{total:1,seniors:1}}};const days=['2026-09-28','2026-09-29','2026-09-30','2026-10-01','2026-10-02','2026-10-03','2026-10-04'];describe('domain rules',()=>{it('detects duplicate shifts',()=>{const c:RosterCell[]=[{id:'a',staffId:'1',date:days[0],shift:'D',version:1},{id:'b',staffId:'1',date:days[0],shift:'N',version:1}];expect(validateRoster(c,s,w,days).some(x=>x.rule==='R1')).toBe(true)});it('warns above 40 hours',()=>{const c:RosterCell[]=days.slice(0,6).map((d,i)=>({id:String(i),staffId:'1',date:d,shift:'D',version:1}));expect(validateRoster(c,s,w,days).some(x=>x.rule==='R3'&&x.severity==='WARNING')).toBe(true)});it('detects invalid ICU certification',()=>{const c:RosterCell[]=[{id:'a',staffId:'1',date:days[0],shift:'N',version:1}];const ss=[{...s[0],icuCertExpiry:'2026-09-01'}];expect(validateRoster(c,ss,w,days).some(x=>x.rule==='R4')).toBe(true)});});
