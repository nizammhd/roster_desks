import {create} from 'zustand'; type State={drawerCellId:string|null;devOffline:boolean;failNextWrite:boolean;anotherEdit:boolean;setDrawer:(id:string|null)=>void;setDev:(key:'devOffline'|'failNextWrite'|'anotherEdit',v:boolean)=>void}; export const useUIStore=create<State>((set)=>({drawerCellId:null,devOffline:false,failNextWrite:false,anotherEdit:false,setDrawer:(id)=>set({drawerCellId:id}),setDev:(key,v)=>set({[key]:v} as Partial<State>)}));
