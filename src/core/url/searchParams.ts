import {z} from 'zod'; export const paramsSchema=z.object({week:z.string().regex(/^\d{4}-W\d{2}$/).default('2026-W39'),ward:z.enum(['ICU','WARD-A','WARD-B']).default('ICU'),role:z.enum(['ALL','SENIOR','JUNIOR']).default('ALL'),search:z.string().default('')}); export function parseParams(sp:URLSearchParams){const r=paramsSchema.safeParse({week:sp.get('week')??undefined,ward:sp.get('ward')??undefined,role:sp.get('role')??undefined,search:sp.get('search')??undefined});return r.success?r.data:paramsSchema.parse({});}
