export const HOSPITAL_TIMEZONE = 'Asia/Kolkata';
export function dateKey(d: Date): string { return new Intl.DateTimeFormat('en-CA',{timeZone:HOSPITAL_TIMEZONE,year:'numeric',month:'2-digit',day:'2-digit'}).format(d); }
export function isoWeekDates(week:string): string[] { const [,w]=week.split('-W').map(Number); const year=Number(week.slice(0,4)); const jan4=new Date(Date.UTC(year,0,4)); const day=jan4.getUTCDay()||7; const mon=new Date(jan4); mon.setUTCDate(jan4.getUTCDate()+(w-1)*7-(day-1)); return Array.from({length:7},(_,i)=>{const d=new Date(mon); d.setUTCDate(mon.getUTCDate()+i); return d.toISOString().slice(0,10);}); }
export function currentIsoWeek(): string { const d=new Date(); const day=new Date(Date.UTC(d.getUTCFullYear(),d.getUTCMonth(),d.getUTCDate())); const th=new Date(day); th.setUTCDate(day.getUTCDate()+(4-(day.getUTCDay()||7))); const y=th.getUTCFullYear(); const jan4=new Date(Date.UTC(y,0,4)); const week=Math.ceil((((th.getTime()-jan4.getTime())/86400000)+1)/7); return `${y}-W${String(week).padStart(2,'0')}`; }
